# 패치 릴리즈 노트

## Step 33 - 회원현황 레이아웃·상태 집계

- 회원 추가/회원 검색 카드 레이아웃 재구성
- 전체 인원에서 탈퇴 제외
- 전체 = 정상 활동중 + 경고로 집계
- 활동중 필터는 경고 회원을 제외한 정상 활동 회원만 표시
- 모바일 반응형 재정리
- PWA 캐시 v19

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 32 - 회원 생일 + 이달의 생일자

- 회원 정보에 생일 추가
- 최초 가입 시 생일 필수 입력
- 관리자가 회원 생일 수정 가능
- 대시보드에 이달의 생일자 카드 추가
- 인기벙 TOP3 / 참석 TOP5 / 이달의 생일자 3카드 구성
- 활동중 회원만 생일자 카드에 표시
- PWA 캐시 v18

DB 변경: `members.birthday date` 추가  
환경변수 변경: 없음

---


## Step 31 - UX/UI Polish

- 모바일 메뉴를 홈/모임/회원/더보기 4개 구조로 개선
- 더보기 Bottom Sheet 추가
- 모임관리 3단계 진행 Stepper 추가
- 회원 카드 관리 버튼을 ⋯ 메뉴로 정리
- notice를 상단 Toast UI로 개선
- 회원 검색 Empty State 추가
- PWA 캐시 v17

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 30 - 회원 상태 인원 + 월별 참석현황 정렬

- 회원현황 상태 필터에 전체/활동중/경고/탈퇴 인원수 표시
- 월별 참석현황 7개 컬럼 헤더에 오름/내림차순 정렬 추가
- 최근 참석 기록이 없는 회원은 날짜 정렬 시 하단 유지
- PWA 캐시 v16

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 29 - 관리자 회원 비밀번호 재설정

- 회원 상세에 관리자용 로그인 계정 관리 영역 추가
- 일반회원 비밀번호 서버 측 재설정 기능 추가
- 제작자는 관리자/일반회원 재설정 가능, 관리자는 일반회원만 가능
- 제작자/본인/미가입 계정 보호
- 비밀번호 값은 변경 이력에 저장하지 않음
- PWA 캐시 v15

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 28 - 리팩토링 및 안정성 점검

- 데이터 조립/최근 참석일 계산 반복 연산 감소
- 중복 상태 초기화 제거
- 사용하지 않는 과거 대시보드 CSS 정리
- Service Worker를 정적 리소스 중심 캐시로 변경
- PWA 캐시 v14

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 27.1 - 대시보드 랭킹 시각화

- `모임별 참석 규모` 제거
- 활동중 회원 대비 참석률 기준 `인기벙 TOP 3` 추가
- 회원별 `이번 달 참석 TOP 5` 카드 추가
- 대시보드 레이아웃을 핵심지표 → 랭킹 → 관리항목 순서로 재구성
- 모바일 1열 반응형 최적화
- PWA 캐시 v13

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 27 - 모임 중심 대시보드

- 대시보드를 모임 정보 중심으로 전면 재구성
- 월 모임/총 참석/참여 회원/총 벙비 시각 카드 추가
- 모임별 참석 규모 막대 시각화 추가
- 관리 필요를 경고 회원 바로가기 버튼으로 변경
- 비용 입력 진행률 및 미입력 모임 바로가기 추가
- 이번 달 운영 통계를 모임 관리 상단의 간소화 요약으로 이동
- PWA 캐시 v12

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 26.1 - 회원현황 상세 버튼 정리

- 회원 카드의 중복 `상세` 버튼 제거
- 닉네임 클릭으로 회원 상세 확인 기능 유지
- 관리자/제작자 운영 버튼은 그대로 유지
- PWA 캐시 v11

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 26 - 회원현황 툴바 및 변경 이력 메뉴

- 회원 검색/정렬을 한 행으로 재배치
- 전체/활동중/경고/탈퇴 상태 버튼을 별도 행으로 정리
- 모바일 390px 이하 상태 버튼 2x2 대응
- 최근 변경 이력을 독립 `변경 이력` 메뉴로 분리
- 모바일 메인 메뉴 6개를 3x2 구조로 조정
- PWA 캐시 v10

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 25 - 모임 참석자·게스트 정렬

- 참석자: 선택회원 먼저 / 닉네임 / 입장일 / 최근 참석일 정렬
- 게스트: 닉네임 / 추가순 정렬
- 기존 참석자 검색 및 일괄 선택 기능 유지
- 모바일 정렬 UI 최적화
- PWA 캐시 v9

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 24 - 회원 관리자 기능 강화

- 관리자/제작자 회원 닉네임 수정
- 로그인 연결 회원의 profiles 닉네임 동시 변경
- 관리자 일반회원 완전 삭제
- 제작자 일반회원/관리자 완전 삭제
- owner 및 현재 로그인 본인 삭제 보호
- 닉네임/입장일/최근 참석일 정렬 추가
- 서버 API에서 관리자 권한 재검증
- PWA 캐시 v8

DB 변경: 없음  
환경변수 변경: 없음

---


## Step 23 - 권한별 사용방법 메뉴

- 메인 메뉴에 `사용방법` 탭 추가
- 로그인 권한에 따라 일반회원 / 관리자 / 제작자 안내 자동 분기
- 공통 기능, 정산 공유, 비용 배분 예시 안내
- 관리자 회원 추가·탈퇴·복귀·입장일 수정·백업 안내
- 제작자 관리자 지정·해제 및 보안 안내
- 모바일 하단 메뉴 5개 탭 대응
- PWA 캐시 v7

DB 변경: 없음  
환경변수 변경: 없음

---


프로젝트: 강서구 찐친만들기 현황관리  
기준 버전: Step 22.2

---

## Step 22.2 - 카카오톡 정산 공유 형식 고정

### 변경
- 정산 공유 형식을 `카톡 꾸밈형` 하나로 고정
- `기본형 / 카톡 꾸밈형 / 간단형` 선택 드롭다운 제거
- `카카오톡 정산 공유` 버튼만 표시
- 선입금 회원 표시 유지
- 게스트 표시 유지
- 실제 줄바꿈 유지
- PWA 캐시 v6

### DB 변경
없음

### 환경변수 변경
없음

---

## Step 22.1 - 정산 공유 줄바꿈 수정

### 수정
기존 공유 메시지에서 줄바꿈이 실제 엔터가 아니라 `\n` 문자로 표시되는 문제 수정

### 적용 범위
- Web Share API
- 클립보드 복사
- 정산 공유 문구

### DB 변경
없음

### 환경변수 변경
없음

---

## Step 22 - 관리자 회원 추가

### 신규 기능
관리자와 제작자가 `회원 현황`에서 회원을 직접 추가할 수 있습니다.

입력 항목:
- 닉네임
- 입장일

### 계정 연결 방식
관리자가 추가한 회원은 회원 명단에 먼저 등록됩니다.

이후 회원이 동일 닉네임으로 `최초 가입`하면:
- 기존 members 데이터 유지
- 기존 입장일 유지
- 기존 참석 이력 유지
- Supabase Auth 계정 생성
- profiles 자동 연결

### 추가
- 중복 닉네임 차단
- 회원 추가 활동 로그
- 모바일 입력 UI 지원
- PWA 캐시 v4

### DB
`supabase/step22_admin_add_member.sql` 1회 실행 필요

---

## Step 21.1 - 모바일 하단 메뉴 수정

### 문제
모바일에서 하단 탭 메뉴가 화면 대부분을 덮는 현상 발생

### 원인
모바일 `.mainTabs`에서 `position: fixed` 사용 시 데스크톱의 `top` 값과 모바일의 `bottom` 값이 동시에 적용

### 수정
- `top: auto`
- safe-area 대응
- 하단 메뉴 최대 높이 제한
- 본문 하단 여백 조정
- PWA 캐시 갱신

### DB 변경
없음

---

## Step 21 - 사용자 배포용 정리

### 개선
- owner 존재 여부 자동 확인
- owner가 없을 때만 `초기 운영자 설정` 노출
- owner 설정 완료 후 메뉴 자동 숨김
- 신규 가입 입장일을 Asia/Seoul 기준으로 저장
- JSON 전체 백업은 관리자 이상에게만 표시
- JSON 백업에서 profiles 제거
- PWA 캐시 갱신

### DB 변경
없음

---

## Step 20 - PWA / 정산 공유

### 신규 기능
- PWA 설치 지원
- manifest 추가
- service worker 추가
- 앱 아이콘 추가
- 앱 설치 버튼
- 모바일 UX 개선
- 정산 공유 기능

### 정산 공유
초기 버전에서는:
- 기본형
- 카톡 꾸밈형
- 간단형

3가지 템플릿을 지원

현재 Step 22.2에서는 카톡 꾸밈형 하나로 통합되었습니다.

---

## Step 19 - 통계 / 백업 / 공유

### 신규 기능
- 월 참석 TOP 5
- 월별 CSV 내보내기
- JSON 백업
- 정산 공유

---

## Step 18 - UX 개선

### 신규 기능
- 내 활동 상세
- 회원 상세
- 비밀번호 변경
- 참석자 전체 선택
- 참석자 전체 해제
- 이전 모임 참석자 불러오기
- 대시보드 관리 알림
- 운영 통계

---

## Step 17 - 관리 기능 및 로그

### 변경
- 선입금 표시는 비용 배분 영역으로 제한
- `남은 부담금` 명칭 제거
- `벙비 합계` 사용

### 신규 기능
- 모임 수정
- 모임 삭제
- 비용 유효성 검사
- 활동 로그

### DB
`activity_logs` 테이블 추가

---

## Step 16 - 모임 UX

### 신규 기능
- 모임 상세보기
- 참석자 검색
- 모임 복사
- 모바일 최적화

---

## Step 15 - 셀프 가입 및 공통 UX

### 변경
- 관리자 계정 생성 방식 제거
- 회원이 본인 닉네임과 비밀번호로 최초 가입
- 기존 회원 닉네임과 자동 연결
- 신규 가입자는 기본 `user`
- 제작자 최초 설정 기능
- 역할별 공통 화면 구성

### 수정
참석 해제 시:
- 특정값
- 선입금

자동 정리

---

# 현재 권한 구조

| 기능 | 일반회원 | 관리자 | 제작자 |
|---|:---:|:---:|:---:|
| 로그인/가입 | O | O | O |
| 대시보드 조회 | O | O | O |
| 모임 생성/수정/삭제 | O | O | O |
| 참석자 관리 | O | O | O |
| 비용 배분 | O | O | O |
| 정산 공유 | O | O | O |
| 회원 현황 조회 | O | O | O |
| 회원 추가 | X | O | O |
| 회원 탈퇴/복귀 | X | O | O |
| 입장일 수정 | X | O | O |
| 운영자 백업 JSON | X | O | O |
| 관리자 지정/해제 | X | X | O |

---

# 배포 방법

```powershell
git add .
git commit -m "Update release"
git push
```

GitHub와 Vercel이 연결된 경우 push 후 자동 배포됩니다.

---

# 주의사항

- SQL 파일이 포함된 버전은 Supabase SQL Editor에서 1회 실행해야 합니다.
- `.env.local`은 Git에 커밋하지 않습니다.
- `SUPABASE_SERVICE_ROLE_KEY`는 서버 전용입니다.
- `CREATOR_SETUP_KEY`는 제작자 초기 설정용 비밀값입니다.
- 모바일/PWA에서 이전 화면이 남아 있으면 앱 재실행 또는 새로고침을 수행합니다.
